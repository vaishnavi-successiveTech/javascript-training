// Find Error in below questions and rectify them

const myArray = [1, 2, 3, 4]; // const is better to use
for (let i = 0; i < myArray.length; i++) { // = should be removed 
   console.log(myArray[i]);
}